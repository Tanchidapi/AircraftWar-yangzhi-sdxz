name: Build Android APK

# 触发条件：push到main/master分支，或手动触发
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:  # 允许手动触发

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # 1. 检出代码
    - name: Checkout code
      uses: actions/checkout@v4

    # 2. 设置 JDK 17
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    # 3. 设置 Gradle
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3

    # 4. 进入Android项目目录
    - name: Change to Android project directory
      run: cd AircraftWarAndroid

    # 5. 生成 Gradle Wrapper
    - name: Generate Gradle Wrapper
      run: |
        cd AircraftWarAndroid
        if [ ! -f gradle/wrapper/gradle-wrapper.jar ]; then
          gradle wrapper --gradle-version 8.0
        fi

    # 6. 赋予 gradlew 执行权限
    - name: Grant execute permission for gradlew
      run: cd AircraftWarAndroid && chmod +x gradlew

    # 7. 构建 Debug APK
    - name: Build Debug APK
      run: cd AircraftWarAndroid && ./gradlew assembleDebug --stacktrace

    # 8. 上传 Debug APK 产物
    - name: Upload Debug APK
      uses: actions/upload-artifact@v4
      with:
        name: AircraftWar-debug-apk
        path: AircraftWarAndroid/app/build/outputs/apk/debug/*.apk
        retention-days: 30

    # 9. 构建 Release APK
    - name: Build Release APK
      run: cd AircraftWarAndroid && ./gradlew assembleRelease --stacktrace

    # 10. 上传 Release APK 产物
    - name: Upload Release APK
      uses: actions/upload-artifact@v4
      with:
        name: AircraftWar-release-apk
        path: AircraftWarAndroid/app/build/outputs/apk/release/*.apk
        retention-days: 30